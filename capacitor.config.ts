import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nav-controller-pop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
